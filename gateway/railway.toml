[build]
builder = "nixpacks"
buildCommand = "npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "DATABASE_URL=$DIRECT_URL npx prisma db push --accept-data-loss && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.healthchecks]]
type = "tcp"
port = 18790
